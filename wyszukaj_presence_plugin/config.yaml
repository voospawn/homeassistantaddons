name: wyszukaj presence
version: "1.1.0"
slug: wyszukaj_presence
description: modół skanera sieci do wyszukaj presence w C++ ARP 
startup: services
boot: auto

host_network: true

privileged:
  - NET_RAW
  - NET_ADMIN

map:
  - type: homeassistant_config
    read_only: false

arch:
  - amd64
