name: wyszukaj presence
version: "1.1.6"
slug: wyszukaj_presence
description: modół skanera sieci do wyszukaj presence w C++ ARP 
startup: services
boot: auto

host_network: true

privileged:
  - NET_RAW
  - NET_ADMIN

map:
  - type: homeassistant_config
    path: custom_components/
    path_in_container: /custom
    read_only: false

arch:
  - amd64
